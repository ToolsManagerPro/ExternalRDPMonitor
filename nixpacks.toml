[phases.setup]
cmds = ["python --version"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[phases.build]
cmds = ["echo 'Building Streamlit app...'"]

[start]
cmd = "streamlit run app.py --server.port=$PORT --server.address=0.0.0.0"
